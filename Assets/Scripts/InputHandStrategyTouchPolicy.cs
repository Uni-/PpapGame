using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// this class is created for duct-tape first, refactoring later
public class InputHandStrategyTouchPolicy : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        /*
        for (int i = 0; i < Input.touchCount; i++)
        {
            Touch touch = Input.GetTouch(i);
            inputCoords = new Vector3(touch.position.x, touch.position.y);
            //Debug.Log(touch.position.ToString());
        }
        
        */

    }
}
